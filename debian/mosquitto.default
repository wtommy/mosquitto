# Defaults for mosquitto initscript
# sourced by /etc/init.d/mosquitto
# installed at /etc/default/mosquitto by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS="--daemon --config-file /etc/mosquitto.conf"
